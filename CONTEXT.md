# PandaClaw 领域术语表

> 本文件只是术语表：定义领域语言，不含实现细节。条款的完整机械规则见 `pandaclaw-meeting/SKILL.md` 与插件源码；决策沿革见 `docs/adr/`。

## 会议

一次召集形成的**协作期**：共享成员名册与主持人，可容纳一个或多个议题。会议不是裁决单元——它不投票、不出决议，只聚合人员与时间。现实原型：人大常委会会议（多案一会，逐案表决）、国务院常务会议（多题一会，逐题原则同意）。

## 议题

最小的**裁决单元**：拥有自己完整的文书序列（意见 → 质询 → 答辩 → 草案 → 表决 → 决议）。「一事一案」中的「事」即指议题——该原则约束的是案卷划分，不禁止一场会议容纳多个议题。

## 案卷号（旧称：文号）

一个议题的完整档案编号，格式 `PC-{类型字}〔年份〕{序号}号`。协商回路、三审制轮次、表决前置门、机械计票的**唯一作用域键**。

⚠️ 案卷号不是「会议的编号」：一场会议可以开多个案卷；反过来，一个案卷的档案独立于任何特定会议存续。详见 [ADR-0001](docs/adr/0001-案卷号语义与两层会议模型.md)。

## 总纪要

会议收尾时由主持人合成的汇总文档：分条引用其覆盖的各案卷决议，对应国常会「一份纪要多事项」的现实形态。总纪要是文档合成任务，不是新的裁决程序——它不产生任何新的决定。

## 纪要型（MIN）

**记载性程序**：固化「已经形成的共识」，不产生新决定。现实定义锚点：《党政机关公文处理工作条例》第八条——纪要「适用于记载会议主要情况和议定事项」，动词是「记」；与如实逐记全过程的「会议记录」相区分，纪要只载议定事项。它是六类会议中唯一不产生新决定的类型，价值＝给轻量场景一条便宜的确证通道。

## 确证书

MIN 确认阶段成员的输出单元：`确认`（记录如实）或 `更正：<失真处>`（≤60 字）。回答的问题是「这份记录是否如实」（核验语义），不是「你是否赞成」（决议语义）。对应现实条款「与会单位对议定事项有异议的应在参会期间明确提出，事后不得拒绝落实会议精神」——异议窗口封闭后纪要即成执行依据。与选票的区别见 [ADR-0002](docs/adr/0002-MIN确认阶段的确证书语义.md)。

## 更正循环

出现更正后，主持人修订记录并重新发起确证的**纠错循环**。循环次数上限复用 ≤3，但语义是防死循环的纠错上限——不叫三审制：三审制保护的是「议题不被反复否定」，更正循环处理的是「记录失真」，被挑战的对象不同。

## 程序性批复

用户在验收门或征询呈报处作出的处置只回答「程序往哪走」——放行·采信／打回·再议／终止——不直接产生决议内容。实质修改意见必须由主持人转写为焦点回协商回路，经表决落文；批复不是票、不改变通过规则（与[确证书](#确证书)的核验语义、选票的决议语义均不同）。一般原理：人类裁量只产生程序性处置，实质内容必经协商回路与表决。依据 [ADR-0004](docs/adr/0004-征询模式效力边界与呈报三选制.md)、[ADR-0005](docs/adr/0005-验收门用户角色与程序性批复.md)。

## 监督窗口

用户以民众监督者身份介入会议的时段与通道：任意 ⭐ 阶段可提监督质疑（各征意阶段为主窗，**计票启动即关窗**），归档后转[复审通道](#复审通道)。意见经主持人**代录入板**（标注「代录·用户」），原汁原味不改写，过准入审查但不因批评性被拒，不替代成员产物的前置门计数；效力为输入而非裁决——与[程序性批复](#程序性批复)互补构成用户的「两顶帽子」：会中监督者（实质输入经回路）、门上裁量者（仅程序三选）。依据 [ADR-0006](docs/adr/0006-监督窗口与用户监督角色.md)。

## 关窗预告

⭐ 阶段拟计票前，主持人必须登记的 `warning` 记录（拟计票通报摘要）：把「关窗」从瞬时事件变成**可预示事件**，用户由此获得一拍的响应窗口。存在性由 `pc_tally` 前置门机械强制（无 warning 拒计票），不依赖 SKILL 约束力；只对 ⭐ 阶段生效、MIN 确证豁免。依据 [ADR-0008](docs/adr/0008-监督窗口关窗预告与计票前置门.md)。

## 监督替身

用户未作任何回应输入时，以用户监督立场在 ⭐ 阶段代提监督意见的子代理（标注「代·替身」）。spawn 由**服务层自动完成**——`tally` 门二受阻（有 `warning`、无 `supervision` 记录、计票被挡）时，服务层以专用 preset（`pc-supervisor-standin`）直接创建（seed 空、setup 代码硬编码），不经任何 AI 之手；替身意见异步落板后再次计票放行。意见**不算票**、不替代前置门计数；用户回来自动获得**追认/撤回权**。监督窗口由此形成两阶段：一阶段本人窗、二阶段替身窗（作用于当轮）。触发位点＝计票尝试且用户缺席，用户正常回应时永不抢答。现实同构：公益诉讼的代表人机制。依据 [ADR-0009](docs/adr/0009-用户缺席替身监督与两阶段窗口.md)、[ADR-0010](docs/adr/0010-复审回告闭环与归档后效力处置.md) 第 9 节。

## 席位绑定

成员首次提交产物时「名字→会话」的永久绑定：异会话冒用同名被 NAME_TAKEN 拦截并留审计字段。断线重启的新会话由主持人核实后 `pc_rebind` **认证重绑**——绑定权威转移至新会话，旧会话随之失效（防线对称，防崩溃恢复变冒名后门）；仅限断线恢复，不得用于中途换人。现实同构：代表资格审查委员会的法定确认程序。依据 [ADR-0007](docs/adr/0007-席位认证重绑制.md)。

## 复审通道

已归档案卷的事后纠错通道（ADR-0006 归档后的复审子通道的数据面化）：复审意见落**原案卷**内 `review` 记录（归档后唯一允许的写入面），六阶段状态机（登记→受理→审查→沟通→决议出口→反馈回告）在**原案卷的复审子状态**上推进，`status` 保持 `adjourned` 不复活原卷。复审处理的是**机械门禁检不到的东西**（决议质量/正确性）与**协议盖章放行但留了尾巴**的降级态，不处理机械可检的违规（那归归档门禁拦截）。依据 [ADR-0010](docs/adr/0010-复审回告闭环与归档后效力处置.md)。

## 待复审池

「有备必审」的登记与执行语义：主力档（RES/LEG）归档即入池，**归档位点批量泵**按优先级（征询采信优先）自动逐个 spawn 审查替身出审——不漏任何主力档；次级/弱档（PLA/STR/CON）归档只登记，**用户在场时**以提意见（request）或专项（dispatch docIds）开启；MIN 不产生新决定（「记」非「决」）不入池，其异议走会内更正循环。降级标记唯一＝征询采信（验收 skip 是 RES 默认档正常产物，非降级态；其检验由自动复审覆盖）。泵/排序全由服务层机械执行，无 AI 判断；轮候节律由协议事件驱动，不读墙钟（LLM 本地时钟，ADR-0008 语义延续）。依据 [ADR-0010](docs/adr/0010-复审回告闭环与归档后效力处置.md) 第 7 节。

## 审查替身

复审第③阶段的审查主体：由服务层在 `review` 记录落板后**自动 spawn**（`ctx.agents.create`，preset `pc-reviewer`，seed 空、setup 代码硬编码），不经任何 AI 之手、不受主持人/用户上下文污染。它只接收服务层构造的**结构化审查包**（案卷号/议题/类型/阶段完成情况/决议原文/票况/规约核对结果），**不见任何原始自由文本**；产出「审查意见＋逐条处置清单」直写服务层，是**建议性意见**而非裁量——出口裁量永远在用户。现实同构：常委会工作机构（法工委）的机关式审查。依据 [ADR-0010](docs/adr/0010-复审回告闭环与归档后效力处置.md) 第 4/5 节。

## 异议方公民

归档时**保留会话**（`team_dismiss` 改为仅解散非异议方）的、在原会议中提出过异议（反对票/质询/未采信意见）的成员子代理。复审已触发时唤醒其**原会话**，以**被动陈述人**身份向审查环节陈述原异议论据——不主动开路（归档后无新信息源）、不参与裁量（利害关系人不裁判自己的案子）。现实同构：提出审查建议的组织主体（监督法 §42②）。依据 [ADR-0010](docs/adr/0010-复审回告闭环与归档后效力处置.md) 第 6 节。

## 复审出口

用户以门上裁量者帽子对复审作出的三选：**修订重议**（新案卷关联原卷，原决议效力被取代）、**解释性决议**（原案卷追加标注「解释性决议」的 resolution，效力同级）、**驳回并说明**（原案卷追加 `review-reply` 逐条回告）。三选均不触动原案卷归档状态；审查替身出建议性审查意见后呈用户裁定——最终通过/修订**永远由用户决定**。现实同构：监督法 §43 三出口（撤销议案/修改废止建议/法律解释议案）。依据 [ADR-0010](docs/adr/0010-复审回告闭环与归档后效力处置.md) 第 8 节。
